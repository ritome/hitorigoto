# Claude Codeへの引き継ぎ書

このファイルをClaude Codeの最初のプロンプトとして渡してください。

---

## 引き継ぎプロンプト（ここからコピー）

こんにちは。ヒトノワスタジオが作る個人開発のPWAアプリ「ワタシノヒトリゴト（ヒトリゴト）」の開発を引き継ぎます。

### プロジェクト概要

体調管理（排便・生理）、スケジュール管理、日々のメモを一つにまとめた、自分専用のPWAアプリです。
詳細は `README.md` を参照してください。

### 現在の状態

すべてのファイルはこのフォルダに揃っています：
- `index.html` - アプリ本体（②Aクリーム配色、全機能実装済み）
- `manifest.json` - PWA設定
- `sw.js` - Service Worker（オフライン対応）
- `icon-192.png` / `icon-512.png` / `icon-180.png` / `icon-maskable-512.png` - アイコン各サイズ
- `icon.svg` / `icon-maskable.svg` - アイコン編集用元データ
- `README.md` - プロジェクト説明

### お願いしたいこと

以下を順番に進めてください。

#### ステップ1：ローカルで動作確認

```bash
# このフォルダでローカルサーバーを起動
python3 -m http.server 8000
# または
npx serve
```

ブラウザで `http://localhost:8000` を開いて、以下を確認：
- スプラッシュ画面が出る
- アプリが正常に表示される
- タスク追加・体調記録・メモ保存が動く
- 開発者ツールの Application タブで Service Worker が登録されているか
- Application → Manifest でPWA情報が正しく読まれているか

#### ステップ2：GitHubリポジトリ作成 & push

1. GitHubで新しいリポジトリ `hitorigoto` を作成（公開）
2. このフォルダで git init、commit、push

```bash
git init
git add .
git commit -m "Initial commit: ワタシノヒトリゴト v1"
git branch -M main
git remote add origin https://github.com/[ユーザー名]/hitorigoto.git
git push -u origin main
```

#### ステップ3：GitHub Pages有効化

`gh` コマンドが使えるなら：
```bash
gh repo edit --enable-pages --pages-branch main
```

または手動で：
- リポジトリの Settings → Pages
- Source: `Deploy from a branch`
- Branch: `main` / `/ (root)`

数分後に `https://[ユーザー名].github.io/hitorigoto/` で公開される。

#### ステップ4：実機テスト

- iPhone Safari で公開URLを開く → 共有 → ホーム画面に追加
- Android Chrome で公開URLを開く → メニュー → ホーム画面に追加（バナーで自動的に促される）
- どちらもフルスクリーン表示・スプラッシュ画面・オフライン動作を確認

### 注意事項

- **データは端末内（localStorage）のみ**で管理。外部送信は一切なし
- **個人の医療データを扱うので、プライバシー配慮を最優先**
- アプリ更新時は `sw.js` の `CACHE_VERSION` をインクリメント（`'hitorigoto-v1'` → `'v2'`）

### 困ったときの判断基準

- バグ → ローカルで再現確認 → 修正 → push
- 機能追加 → 一度ユーザーに相談してから実装
- デザイン変更 → ユーザーに確認

### 開発履歴のコンテキスト

このアプリは何回かの会話を経て以下を決めて作られています：

- **アプリ名**：ホーム画面表示は「ヒトリゴト」、起動時スプラッシュは「ワタシノヒトリゴト」
- **会社名**：ヒトノワスタジオ
- **配色テーマ**：②Aクリーム明るめ（背景 #faf6ee、アクセントはテラコッタ #b8745c）
- **アイコン**：細線の俯瞰波紋（テラコッタ背景 × クリームの線、北欧テイスト）
- **メモタグ**：4種類（💭つぶやき / 💡ひらめき / 📖きょう / ☘️ありがとう）
- **設計思想**：書かなくていい、プレッシャーゼロ、自分のための場所

よろしくお願いします。

---

## このプロンプト以外に伝えること

- 公開URLが決まったら知人に共有
- 月1回くらいバックアップを取る習慣をつける
- 何か機能追加したくなったら、いつでもこのチャットに戻ってきてOK
