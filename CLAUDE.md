# CLAUDE.md — ワタシノヒトリゴト 開発コンテキスト

このファイルは Claude Code が毎回読み込む設計／運用メモです。READMEは利用者向け、これは**実装判断のための作業者向け**です。仕様変更や重要な決定をしたら、その都度このファイルを更新してください。

---

## 1. アプリの正体

- **正式名称**：ワタシノヒトリゴト（起動時のスプラッシュ表記）
- **ホーム画面表示**：ヒトリゴト（manifest.json `short_name`）
- **作者**：ヒトノワスタジオ（個人開発のブランド）
- **GitHub**：https://github.com/ritome/hitorigoto
- **本番URL**：https://ritome.github.io/hitorigoto/
- **説明書（Web）**：https://ritome.github.io/hitorigoto/manual.html
- **説明書（PDF）**：https://ritome.github.io/hitorigoto/ワタシノヒトリゴト_説明書.pdf

体調管理（排便・生理）、スケジュール管理、日々のメモを **一つにまとめた個人用 PWA**。単一HTML、依存ライブラリなし、Vanilla JS、Service Workerでオフライン対応、localStorage でローカル保存のみ。

---

## 2. 設計思想（やる／やらない）

### やる
- 自分のための記録（誰にも見せない、SNSではない）
- プレッシャーゼロ（書かなくていい、欠けてもいい）
- 北欧テイストで上品に、優しいトーン
- データの主権はユーザーに（バックアップは手動JSON）

### やらない（重要）
- **外部にデータを送信しない**。サーバー、クラウド、analytics、トラッキング、フォント以外のCDN、すべて禁止
- **医療アドバイスや診断機能は実装しない**。生理予測は単純な周期平均のみ、判定や警告はしない
- **通知や催促を強くしない**。バックアップ催促バナーですら優しいトーンに留める（`danger`色は廃止済み）
- **②Aクリーム配色を崩さない**。新色を増やすときは要相談
- **依存ライブラリやフレームワークを入れない**。Vanilla JS縛り
- **複雑な状態管理を導入しない**。`state` グローバル + `saveData()` で十分

---

## 3. デザイントークン

`index.html` の `:root` で定義。**新色追加・既存色変更は事前確認**。

| 用途 | 変数 | 値 | 備考 |
|---|---|---|---|
| 背景 | `--bg` | `#faf6ee` | クリーム |
| カード | `--card` | `#ffffff` | |
| 文字 | `--ink` / `--ink-soft` / `--ink-mute` | `#2c2a26` / `#6b6862` / `#9c988f` | 本文／補助／極薄 |
| 罫線 | `--line` / `--line-strong` | `#ece8e0` / `#d8d2c5` | |
| アクセント | `--accent` | `#b8745c` | テラコッタ（メインカラー、予定・バックアップ・トースト等） |
| アクセント濃 | `--accent-deep` | `#a35840` | 強調文字 |
| アクセント淡 | `--accent-soft` | `#f5e3d2` | バッジ背景 |
| 排便 | `--bowel` / `--bowel-soft` | `#5a7048` / `#e0e8d6` | 穏やかなグリーン（クローバーSVGも同色） |
| 生理 | `--period` / `--period-soft` | `#b56868` / `#f5dcd6` | 柔らかな朱 |
| 頭痛 | `--headache` / `--headache-soft` | `#c47878` / `#f5dcd6` | くすみローズ。`--danger`と近いがバナーには使わない（dangerは削除文字色専用） |
| タスク | `--task` / `--task-soft` | `#6b87a8` / `#e3eaf2` | 落ち着いた青 |
| メモ | `--memo` / `--memo-soft` | `#8b6f9c` / `#ece4f0` | やわらかい紫 |
| 警告 | `--warn` | `#d4a55a` | バナー類（琥珀系） |
| 危険 | `--danger` | `#c47878` | 削除ボタンのみ。バナーには使わない |

**角丸**：`--radius: 14px` / `--radius-lg: 20px`

**フォント**：iOS / macOS は -apple-system、それ以外は Hiragino / Yu Gothic フォールバック。manual.html のみ Noto Serif JP / Noto Sans JP を Google Fonts から読み込む（説明書のブランド表現として例外的に許容）。

---

## 4. データ構造

### localStorage キー

```
'hitorigoto_v1'  ← state全体をJSON.stringifyで格納
```

このキー名は **互換性の都合で v1 のまま**。データ構造のメジャー破壊変更があれば v2 に上げる、というルール。

### state スキーマ

```js
{
  bowelRecords:    { [dateStr]: { passed: bool, bleeding: bool, memo: string } },
  periodRecords:   [ { id, start, end|null, flow, memo } ],
  headacheRecords: { [dateStr]: { occurred: bool, level: string|null, memo: string } },  // v5で追加
  tasks:           [ { id, date, time, title, done } ],
  events:          [ { id, date, time, title, location, memo } ],   // v2で追加
  recurringTasks:  [ { id, title, time, days: [0..6] } ],
  recurringDone:   { [`${dateStr}_${recurringId}`]: bool },
  memos:           [ { id, date, time, tag, text } ],
  lastBackup:      null | ISOString
}
```

- `dateStr` は常に `YYYY-MM-DD`（`ymd()` 関数で生成）
- `time` は `HH:MM` 文字列（任意項目は空文字）
- `tag` は ['つぶやき' | 'ひらめき' | 'きょう' | 'ありがとう'] のいずれか
- `flow` は ['少ない' | '普通' | '多い'] のいずれか
- 頭痛 `level` は ['軽い' | '中くらい' | '重い'] のいずれか。`occurred: false` のときは `null`

### 互換性ルール（重要）

`loadData()` は `Object.assign(JSON.parse(JSON.stringify(defaultData)), parsed)` で読み込む。これにより：
- 旧バージョンのバックアップ（新フィールドが無い）も問題なく読める
- **新フィールドを追加するときは `defaultData` に必ず初期値を書く**（これが互換性の生命線）
- フィールドを削除する場合は、しばらく `state` から参照しないだけにして、データは残す（戻れる余地）

### バックアップ仕様

- エクスポート：`exportData()` が `state` 全体を JSON ファイル化、ダウンロード
- ファイル名：`hitorigoto_backup_YYYY-MM-DD.json`
- インポート：`importData()` で全置換（既存データは上書き、確認ダイアログあり）
- インポート時の検証：`bowelRecords|periodRecords|tasks|memos|events` のいずれかが存在すれば受理

---

## 5. ファイル構成

```
hitorigoto/
├── index.html                       ← アプリ本体（HTML/CSS/JS全部入り）
├── manual.html                      ← Web版説明書（Noto Serif JP使用、北欧テイスト）
├── ワタシノヒトリゴト_説明書.pdf  ← PDF版説明書（ダウンロード用）
├── manifest.json                    ← PWA設定（name, theme-color, icons）
├── sw.js                            ← Service Worker（キャッシュ＋オフライン）
├── icon.svg / icon-maskable.svg     ← 編集用元データ
├── icon-192.png / 512.png / 180.png / -maskable-512.png  ← 各サイズ
├── README.md                        ← 利用者向け概要
├── 引き継ぎ書.md                   ← 初回セットアップ用（過去ログ）
└── CLAUDE.md                        ← このファイル
```

**ファイル名規則**：日本語ファイル名はGitHub Pagesでも問題なく配信される（`Content-Type: application/pdf` 確認済み）。URLでは自動的にパーセントエンコードされる。

---

## 6. UI 全体構成

`.container` (max-width 480px) に以下を縦並び：

1. **ヘッダー**：日付・曜日 + 「最終バックアップ：◯日前」 + アプリ名（右上）+ バックアップ催促バナー（条件表示）
2. **今日のタスク**：タスクリスト + 「＋ タスクを追加」 + 「＋ 予定を追加」
3. **今日の予定**（その日に予定がある時のみ表示）
4. **今後の予定**（明日〜14日先に予定がある時のみ表示、相対日表示）
5. **体調記録**：1段目「排便を記録」「生理を記録」、2段目「頭痛を記録」+ 今日の状況表示
6. **今日のヒトリゴト**：4タグ + テキストエリア + 保存ボタン + 今日のメモ一覧
7. **カレンダー**：月ナビ + フィルタータブ([すべて][体調][予定][メモ]) + 日付グリッド + 凡例
8. **サマリー**：次回生理予測 + 今月の排便日数
9. **設定・バックアップ**：「📥 すぐバックアップ」（目立つ）+ 各種設定 + 既存「バックアップを保存」

### モーダル

| モーダルID | 用途 |
|---|---|
| `taskModal` | タスク追加・編集 |
| `eventModal` | 予定追加・編集 |
| `bowelModal` | 排便記録 |
| `periodModal` | 生理記録 |
| `headacheModal` | 頭痛記録（有無を選ぶとレベル行が動的に表示／非表示） |
| `recurringModal` | 繰り返しタスク管理 |
| `dayModal` | 日付詳細（カレンダーから） |
| `memoHistoryModal` | メモ振り返り |
| `memoEditModal` | メモ編集 |
| `helpModal` | 使い方 |

### 保存フィードバック（v3で追加）

- すべての保存処理で `showToast('保存しました ✓')` を呼ぶ：saveTask / saveEvent / saveBowel / savePeriod / saveMemo / saveMemoEdit / addRecurring
- 「ありがとう」タグのメモ保存時のみ、追加で `spawnClovers()` でブランドカラー(#5a7048)のクローバーSVGが4枚舞う

### メモタグ（拡張禁止）

| 絵文字 | タグ名 | 用途 |
|---|---|---|
| 💭 | つぶやき | 短いひとこと |
| 💡 | ひらめき | アイデア・気づき |
| 📖 | きょう | 日記、今日のこと |
| ☘️ | ありがとう | 感謝・嬉しかったこと（保存時に演出あり） |

タグの追加・削除はユーザーの世界観を壊すので **要相談**。

---

## 7. 開発フロー

### ローカル確認

```bash
# プロジェクト直下で
python3 -m http.server 8000
# → http://localhost:8000 を開いて動作確認
# Service Worker 更新を確認するときは Cmd+Shift+R で強制リロード
```

### リリース手順（ほぼ毎回これ）

1. `index.html` などを編集
2. **`sw.js` の `CACHE_VERSION` をインクリメント**（必須。忘れると既存ユーザーが旧版のまま）
3. ローカルで動作確認
4. `git add . && git commit -m "..."` （Co-Authored-By 行を含める）
5. `git push origin main`
6. GitHub Pages が自動ビルド（30〜40秒）
7. `https://ritome.github.io/hitorigoto/` で実機確認

### コミットメッセージ方針

- 1行サマリー：「〇〇を追加」「〇〇を修正」など、変更の主旨を簡潔に
- 本文：何を変えたか箇条書き
- 「なぜ」を残したい変更は本文に書く（後でCLAUDE.md にも転記）

### `sw.js` 互換性

- `CACHE_FILES` に新ファイルを追加した場合、必ず `CACHE_VERSION` も更新する
- 削除したファイルは `CACHE_FILES` から外せばよい（古いキャッシュは `activate` で消える）

---

## 8. バージョン履歴

実装と決定事項のログ。新機能追加・仕様変更があったらここに追記。

| SW版 | コミット | 日付 | 内容 |
|---|---|---|---|
| v1 | `25007d7` | 2026-05-02 | 初版（タスク・体調・メモ・カレンダー・繰り返し・JSONバックアップ・PWA一式） |
| — | `f39f2e5` | 2026-05-02 | 説明書（manual.html + PDF）を追加 |
| v2 | `27cc512` | 2026-05-02 | スケジュール機能：events データ追加。今日の予定／今後14日の予定セクション。日付詳細モーダルから直接タスク／予定追加。カレンダーにテラコッタの予定ドット追加 |
| v3 | `c4962c5` | 2026-05-02 | 第1弾改善：ヘッダーに最終バックアップ表示、催促バナーを優しく、設定最上部に「すぐバックアップ」ボタン、カレンダーに [すべて][体調][予定][メモ] フィルタ、保存トースト導入、ありがとうタグでクローバー演出（emoji） |
| v4 | `b5a49ed` | 2026-05-02 | クローバーを emoji ☘ から `--bowel` カラーのSVGに変更（OS依存色を排除） |
| v5 | `f3a35d9` | 2026-05-08 | 頭痛記録を追加（headacheRecords、`--headache`色、有無＋3段階レベル＋メモ、体調フィルターに含む、record-buttonsを2段化） |
| v6 | （次のpushで採番） | 2026-05-08 | iPhoneでメモタグ「ありがとう」が見切れる問題を修正。`.memo-tags` を横スクロールから `flex-wrap: wrap` に変更（小画面で2段折り返し表示） |

---

## 9. 既知の判断・運用メモ

### バックアップ未実施時のバナー

v3で「まだバックアップがありません」のwarnバナーは廃止した。新規ユーザーへの威圧感を避けるため。代わりに設定セクション最上部の「📥 すぐバックアップ」ボタンと、ヘッダー直下の `.date-backup` 表示が無いこと（無言の表示）で訴求する。**復活させる場合はユーザーに相談すること**。

### 警告色の使い分け

- バックアップ催促バナー：`warn` クラスのみ（琥珀系）。`danger`（赤系）は使わない
- 削除ボタン：`--danger` 文字色OK
- アラート的な使い方は基本しない

### 過去予定の扱い

「今後の予定」セクションは今日 +1日〜+14日のみ表示。**過去の予定はデータとしては残るが UI には出さない**（カレンダー詳細モーダルからは見える）。データを消す機能は意図的に未実装。

### 生理予測の精度

`predictNextPeriod()` は直近4回の平均周期から計算。サンプルが少ないとデフォルト周期28日。**機械学習や複雑な統計を入れる予定なし**（医療デバイス化を避ける）。

### XSS 対策

メモやタスクのテキスト挿入は **必ず `textContent`** を使用。`innerHTML` は静的なテンプレートのみ。新規UI追加時は `buildEventItem` のような DOM API ベースのビルドを参考にする。

### ファイル名の日本語

`ワタシノヒトリゴト_説明書.pdf` のような日本語ファイル名は GitHub Pages で動作確認済み。新たに日本語ファイル名を使う場合も問題ないが、ASCIIで済むものはASCIIにしておく方が安全。

---

## 10. 今後の判断指針

仕様追加が来たとき、以下に照らして判断：

1. **「自分のため」「プレッシャーゼロ」を強める方向か**（書く義務感を増やすなら却下寄り）
2. **データを端末外に出さないで実現できるか**（できなければ却下）
3. **既存の北欧テイストを保てるか**（新色・派手な要素は要相談）
4. **依存追加なしで実装できるか**（できなければ要相談）
5. **既存データとの互換性を保てるか**（`defaultData` 拡張で対応可か）

迷ったら、ユーザー（@ritome）に必ず確認する。勝手に判断しない。

---

*このファイルは生きたドキュメントです。実装と乖離していたら、コードを正として更新してください。*
